<div class="field">
  @if(label)
    <label for="{{ name }}">{{ label }}</label>
  @end
  <input id="{{ name }}" 
    class="{{ type === 'submit' ? 'ui button' : '' }}" 
    {{ $props.except(['label']).toAttrs() }}>
</div>